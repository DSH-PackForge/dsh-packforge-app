{
  "name": "dsh-packforge-app",
  "private": true,
  "version": "0.1.1",
  "type": "module",
  "description": "DSH-PackForge 图形化整合包管理工具（manifest v5 + .dspack / pack-structure v3）",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "pnpm -r --if-present test",
    "build:icon": "node scripts/gen-icon.mjs",
    "build:cli": "node scripts/build-cli.mjs",
    "build:win": "node scripts/build-win.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}